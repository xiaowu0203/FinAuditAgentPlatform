<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.finaudit.agentcore.mapper.ExpenseAttachmentMapper">

    <!-- 批量新增：单条多行 INSERT（CLAUDE.md §5.9），created_at/updated_at 走数据库默认值，deleted 显式写 0 -->
    <insert id="insertBatch">
        INSERT INTO expense_attachment
        (tenant_id, reimb_id, file_record_id, file_type, ocr_status, deleted)
        VALUES
        <foreach collection='list' item='a' separator=','>
            (#{a.tenantId}, #{a.reimbId}, #{a.fileRecordId}, #{a.fileType}, #{a.ocrStatus}, 0)
        </foreach>
    </insert>

</mapper>
